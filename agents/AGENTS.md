# AGENTS.md — WisperVoice Agent Memory

> **For Muse, Claude, Cursor, and any future agent.** This is the shared brain. Read this first, update it after every change. Keep it short, factual, linked.

---

## Project

**WisperVoice** — open-source WisprFlow clone for macOS (SwiftUI, MenuBarExtra, `LSUIElement`). Speak with `⌥Space` or `Fn×2` in *any* app, see live glass pill, auto-edits, paste at cursor. `wisperVoice/WisperVoice.xcodeproj`, `macOS 14+`, `Swift 5.9`, `Xcode 16.3`.

- **App:** `WisperVoice/WisperVoiceApp.swift` (`MenuBarExtra` + `Window(id:main)` + `Settings` + `NSStatusItem` fallback + `SMAppService`)
- **Website:** `website/` (Vite 5 + React 18 + Tailwind 3.4 → `website/dist/`)
- **Tests:** `WisperVoiceTests/` (9 suites, logic bundle, `xctest` direct run)
- **Repo meta:** `.github/workflows/ci.yml` + `release.yml`, `../README.md`, `../plan/ROADMAP.md`, `../plan/PM_PLAN.md`

## What's Done (2026-08-06 → 2026-08-20)

| When | What | Files |
|---|---|---|
| 08-06 | Scaffolded native app, `AVAudioEngine → WAV`, `SFSpeechURLRecognitionRequest` + Whisper `multipart`, `AX`→`Cmd+V`, history, permissions | `WisperVoice/*`, `project.pbxproj` |
| 08-06 | Build fixed (`SFSpeechURLRecognitionRequest`, `AXValue`, `SettingsLink` → macOS 14) → `BUILD SUCCEEDED` | `TranscriptionService`, `TextInjector`, `project.pbxproj` |
| 08-06 | Tests 9 suites, `TranscriptionService.polish` + Whisper + History + Permissions + Audio + TextInjector + Hotkey + Overlay + Dictation + App | `WisperVoiceTests/*.swift`, `project.pbxproj` (test target) |
| 08-20 | Pill reliability: `OverlayWindow.sharedInstance` singleton, `show` on main, level `.floating` | `Managers/OverlayWindow.swift` |
| 08-20 | Live dictation: `DictationManager.liveTranscript` + `SFSpeechAudioBufferRecognitionRequest` partials, `×` close | `Managers/DictationManager.swift`, `OverlayWindow.swift` |
| 08-20 | Local models: `ModelManager` (`ggml-tiny/base/small`, `Application Support/WisperVoice/models`) + `ModelManager` UI | `Managers/ModelManager.swift`, `Views/SettingsView.swift` |
| 08-20 | Always-visible bar + main window + Open at Login (`SMAppService`) | `WisperVoiceApp.swift`, `Views/SettingsView.swift` |
| 08-20 | HIG polish: `MenuBarView` (gradient orb, pulse, `matchedGeometryEffect`, `sensoryFeedback`), `SettingsView` (`Form(.grouped)`), `OverlayView` (glass capsule) | `Views/*`, `Managers/OverlayWindow.swift` |
| 08-20 | TTS: `TTSManager` (Piper/Coqui stub, `tts/` dir), `../plan/ROADMAP.md` updated | `Managers/TTSManager.swift` |
| 08-20 | AI switching (Vercel SDK style): `AIModelProvider` (7 providers, `AIProviderRegistry`, `AIModel`, `AISettingsKeys`) + `TranscriptionService` unified dispatch, `ModelManager` stt/tts selection, `SettingsView` pickers | `Managers/AIModelProvider.swift`, `Managers/ModelManager.swift`, `Managers/TranscriptionService.swift` |
| 08-20 | Website + GitHub + README: Vite site, `ci.yml`/`release.yml`, `README` 13 kB, `LICENSE`, `CONTRIBUTING`, etc. | `website/`, `.github/`, `README.md` |
| 08-20 | **Fix pass (7 PM issues): Dock+menu bar always visible (`LSUIElement false`, `activationPolicy .regular`, `NSStatusItem.isTemplate`, `DockMenu`, `screensDidWake`), pill Copy (`doc.on.doc` + `⌘C` + `×`), auto-stop after 5s silence (RMS VAD + `@AppStorage autoStopAfterSilence/Seconds`), onboarding walkthrough, clipboard/history tab — `BUILD/TEST SUCCEEDED` | `WisperVoiceApp.swift`, `OverlayWindow.swift`, `AudioRecorder.swift`, `DictationManager.swift`, `Views/OnboardingView.swift`, `Views/ClipboardHistoryView.swift`, `Views/SettingsView.swift`, `Views/MenuBarView.swift`, `project.pbxproj`, `Info.plist` |
| 08-20 | PM spec + CTO plan: `PM_SPEC_FIXES.md`, `../plan/CTO_PLAN.md` (no pbxproj change per spec, now patched to include new views) | `PM_SPEC_FIXES.md`, `../plan/CTO_PLAN.md` |
| 08-08 | **Gatekeeper answer + download modal:** `scripts/install.sh` (curl|bash) — CLI downloads never get the quarantine xattr, so installs open with NO warning and no paid account (verified end-to-end from the raw GitHub URL); surfaced on /releases page (copyable card) + release notes template. Self-signing with own name does NOT help downloaders (Gatekeeper trusts only Apple-issued Developer ID); macOS 15+ removed right-click→Open — the browser path is Settings→Privacy & Security→Open Anyway. Download CTAs now also open a DownloadModal (metallic card, blur backdrop, 180ms scale/fade in+out, Esc/backdrop close): 2 install steps + terminal one-liner w/ Copy + guide link; verified via Playwright (download event + dialog present simultaneously). | `scripts/install.sh`, `website/src/App.jsx`, `.github/workflows/release.yml` |
| 08-08 | **Direct-download CTAs:** hero/mobile/footer Download buttons → `releases/latest/download/WisperVoice-macOS.zip` (the version-less asset release.yml uploads per release = stable latest-URL); one click starts the download, no GitHub page; hero adds "Install guide & versions" → #/releases (quarantine step lives there). | `website/src/App.jsx` |
| 08-08 | **Website /releases route + review fixes:** hash-routed `#/releases` page in App.jsx (site convention: single file; components/ dir is empty) — fetches `GITHUB_API/releases` with the same cache discipline as the star count (15min TTL, stale-on-error fallback), renders release bodies with a tiny built-in markdown subset renderer (##/###, bullets w/ continuation folding, fences, `code`/**bold**; skips the duplicate "## WisperVoice vX" title), asset download buttons (ZIP/DMG + size + count), LATEST/PRE-RELEASE pills, loading skeleton + error card. Routing: hashchange listener in App(); #section anchors still work (post-remount scrollIntoView). Nav+footer link to #/releases. Review findings fixed in same pass: meta description + footer Hold→Press, pre-paint theme script now mirrors ThemeSwitcher fallback (non-canonical values = system; matchMedia outside try), 6 hardcoded GitHub URLs → derived GITHUB_URL/RELEASES_URL. Verified: pnpm build + Playwright screenshots of both routes against live API. | `website/src/App.jsx`, `website/index.html` |
| 08-08 | **Release notes ← CHANGELOG (v0.1.0 shipped + verified):** v0.1.0 published w/ zip+DMG+checksums; downloaded artifact verified adhoc-signed + version-stamped 0.1.0(5). `release.yml` now extracts the `## [X.Y.Z]` section from CHANGELOG.md as the release body (commit list only as fallback) — so "update the release / list features" = write the changelog entry before tagging. Live v0.1.0 release body updated via `gh release edit` with the full 0.1.0 feature list. | `.github/workflows/release.yml` |
| 08-08 | **Release pipeline (v0.1.0):** `release.yml` fixed — CRITICAL: it built with CODE_SIGNING_ALLOWED=NO producing an unsigned binary that Apple Silicon's kernel refuses to run; added ad-hoc sign fallback step (Developer ID path unchanged, activates via secrets). Also: version stamped from tag into Info.plist (env-routed per Actions injection guidance), secret `if:`s via job env, release notes now include the required `xattr -d com.apple.quarantine` step. Added `CHANGELOG.md` (the per-release changeset, user-facing) + `plan/RELEASE.md` playbook (tag `vX.Y.Z` = release button; SemVer; re-cut procedure; notarization upgrade path via repo secrets). Release config verified building locally. First release: tag v0.1.0. | `.github/workflows/release.yml`, `CHANGELOG.md`, `plan/RELEASE.md` |
| 08-08 | **CODE REVIEW SWEEP — 10 confirmed bugs fixed (build 18):** (1) tests were DELETING the real Keychain key via openAIKey didSet → KeychainStore uses in-memory store under XCTest; (2) every launch opened System Settings→Microphone (launch path called the button-variant) → new `requestMicrophonePermissionAtLaunch()` prompts only when notDetermined; (3) live-typing dropped words after recognizer revisions (strict hasPrefix) → word-count alignment fallback; (4) `HotkeyManager.apply` stacked flagsChanged monitors (single Fn read as double-tap) → monitors tracked + removed in unregister(); (5) LegacyDefaults re-imported plaintext key + ran after key read → openAIKey special-cased to Keychain, migrateOnce called first in DictationManager.init, KeychainStore.migrate always deletes plaintext; (6) pill's fixed 480pt window swallowed composer clicks → window now hugs capsule width (also fixes left/right alignment); (7) audio-tap/main race on bufferRequest → closure captures request directly; (8) requestAccessibility raced the AX consent dialog → axPromptedOnce gate; (9) cancelled key probe wrote false .unreachable → Task.isCancelled guard; (10) restored didBecomeActive + 1s-retry status-item recovery. 70 tests pass. | `Managers/{TranscriptionService,DictationManager,HistoryStore,HotkeyManager,PermissionsManager,OverlayWindow}.swift`, `Views/SettingsView.swift`, `WisperVoiceApp.swift` |
| 08-08 | **Keychain + minutes stat + fullscreen-open fix (build 17):** API key moved UserDefaults→macOS Keychain (`KeychainStore` in TranscriptionService.swift, native Security framework — NO third-party package; service fixed `com.wispervoice.dev`, NOT bundle id, which differs in test runner); `DictationManager.openAIKey` now @Published w/ Keychain didSet; one-time `migrate()` deletes the plaintext copy; ALL readers switched (TranscriptionService dispatch, OpenAIWhisperProvider, WhisperAPI_Engine, ModelsPane). `HistoryItem.duration: TimeInterval?` (optional → old JSON decodes) recorded from `recordingStartedAt`; Dictate stats add MINUTES tile (pre-duration items estimated at 150 wpm). Fullscreen Dock-click finally fixed: `.fullScreenAuxiliary` alone insufficient for a titled window at `.normal` level (stayed BEHIND the fullscreen app = "doesn't open") — `present(_:)` raises to `.floating` + orderFrontRegardless; `didResignKeyNotification` observer drops back to `.normal`. 70 tests pass. | `Managers/TranscriptionService.swift`, `Managers/DictationManager.swift`, `Managers/HistoryStore.swift`, `Managers/AIModelProvider.swift`, `Views/SettingsView.swift`, `WisperVoiceApp.swift` |
| 08-08 | **OpenAI-compatible cloud config (build 16):** cloud engine no longer OpenAI-locked — `CloudConfig` (`cloud.baseURL`/`cloud.sttModel`/`cloud.polishModel`, unset→OpenAI defaults so tests asserting whisper-1/gpt-4o-mini stay green) drives `/audio/transcriptions`, `/chat/completions` (polish), AND key validation. Settings: Server URL field + Preset menu (OpenAI; **Groq whisper-large-v3-turbo ~9× cheaper w/ free tier**; Mistral voxtral-mini ~half price), transcription-model field, polish-model field (shown when polish on); key re-probes on server change. Provider renamed "Cloud Whisper" (id stays `openai-whisper` — stored settings reference it); `availableModels` computed so model row shows configured model + server. Research + native-adapter roadmap (Deepgram/AssemblyAI/ElevenLabs) in `plan/CLOUD_PROVIDERS.md`. 70 tests pass. | `Managers/TranscriptionService.swift`, `Managers/AIModelProvider.swift`, `Views/SettingsView.swift`, `plan/CLOUD_PROVIDERS.md` |
| 08-08 | **API key validation + insert-first UX (build 15):** `TranscriptionService.validateOpenAIKey` (GET /v1/models; 200→valid, 401/403→invalid, else→unreachable so network blips never say "wrong key"); Settings key row auto-probes via `.task(id: key)` with 800ms debounce → "Key verified" / "rejected" / "couldn't reach" status row; empty-key + OpenAI engine warns inline in Settings AND Models engine subtitle; `startRecording` FAILS FAST with clear error if OpenAI engine + no key (previously errored only after the user finished speaking). History "Paste" button removed (WisperVoice is the focused app while browsing history — no sensible external cursor). Pill dismiss logic: `scheduleOverlayDismiss()` — 1.2s when text was inserted at cursor, 8s grace WITH Copy visible when insertion couldn't work (autoPaste off / no target / no AX); Copy now also shows in idle so it survives the grace period. 70 tests pass. | `Managers/TranscriptionService.swift`, `Managers/DictationManager.swift`, `Managers/OverlayWindow.swift`, `Views/SettingsView.swift`, `Views/ClipboardHistoryView.swift` |
| 08-08 | **DESIGN SYSTEM PASS — "studio instrument" (build 14):** shared components added to `Theme.swift` (NOT a new file — pbxproj lists sources explicitly, and Theme is already in both targets): `SectionLabel` (uppercase tracked), `Card`, `SettingRow`, `RowDivider`, `StatTile`, `KeyChip`, `Pane` (title + 780pt measured column). Type scale: display/sectionLabel/rowTitle/rowMeta/numeral/statFigure + `cardFill`/`wellFill`/`border` surfaces. **Violet is now the app-wide `.tint`** (system blue read as unbranded default) — supersedes DESIGN_SYSTEM_ALIGNMENT §4 "violet sparing" for interactive/active states; ink still owns text/structure. Dictate screen rebuilt as audio-reactive hero (3 concentric rings scale off `audioLevel`, click-to-toggle) + real stats from HistoryStore (total/today/words). **Copy purge — internal notes were shipping as UI**: removed "Vercel AI SDK style: provider(model)", "legacy", "Kept for backward compat", "Replace stub with real Piper/Coqui engine", "Wispr Flow parity"; TTS honestly labeled preview. **Models screen showed the SAME whisper.cpp models twice** ("Tiny — Active" duplicated) — legacy list dropped, catalog (`sttModels`, a superset) is the only list, grouped by provider. History rows: hover-revealed actions, mono relative time, empty state. 70 tests pass. | `Views/Theme.swift`, `Views/SettingsView.swift`, `Views/ClipboardHistoryView.swift`, `Views/MenuBarView.swift`, `Views/OnboardingView.swift`, `WisperVoiceApp.swift` |
| 08-08 | **LIVE STREAMING FIX + slim pill (build 13):** partials only arrived after pauses because recognition ran SERVER-side (batches) — now `requiresOnDeviceRecognition = true` when supported (+ `taskHint = .dictation`, `addsPunctuation`) → word-by-word streaming like system dictation. Pill slimmed: capsule 40pt tall (was 60) in a 52pt window, padding 14/7, compact recording chip 118pt, × is a small 20pt circle, text 12.5, Copy tightened. 70 tests pass. | `Managers/DictationManager.swift`, `Managers/OverlayWindow.swift` |
| 08-08 | **Pill v3 + picker/scale fixes (build 12):** × button was DEAD while recording (`ignoresMouseEvents` gated on state — now always false; nonactivating panel never steals focus anyway); bars amplified (RMS×2.6, spring) — old scaling made speech (~0.1–0.4 RMS) nearly invisible; "Listening…" removed (dot+bars suffice); pill GROWS via animated capsule inside fixed 480pt window: 150pt (Apple, no text) / 300pt ("Transcribes when you stop", non-Apple) / 460pt (text/result); Apple-only live preview labeled in pill+Settings+onboarding. Blank "Active Model" picker root cause: switching provider left the OLD provider's model id — both provider pickers now route through `selectSTT` with first-ready-model reset. History rows: callout text + regular Copy/Paste. Forms max-width 860/900 centered so the enlarged window doesn't stretch rows. NOTE fullscreen menu bar = macOS design (auto-hides; hover top edge reveals icon; hotkey+pill still work). 70 tests pass. | `Managers/OverlayWindow.swift`, `Views/SettingsView.swift`, `Views/ClipboardHistoryView.swift`, `Views/OnboardingView.swift` |
| 08-08 | **LIVE TEXT ROOT CAUSE + pill v2 + bigger UI (build 11):** live transcript never worked because `AudioRecorder`'s tap wrote the WAV + RMS but NEVER fed buffers to `SFSpeechAudioBufferRecognitionRequest` — added `AudioRecorder.onBuffer` → `bufferRequest.append(buffer)` in `startLiveTranscription`, `endAudio()` on stop/cancel (this also makes live-typing-on-pause actually fire). Pill v2 per user spec: mic circle REMOVED — layout is [red dot + waveform bars] [single-line live text, `.truncationMode(.head)` so newest words tail-follow] [Copy only after recording ends] [×]; 440×64. Window +~45%: min 1080×720, default 1200×800, sidebar 190–220; transcript Copy/Paste buttons mini→regular (main) / small (popover); silence sliders full-width stacked rows. 70 tests pass. | `Managers/AudioRecorder.swift`, `Managers/DictationManager.swift`, `Managers/OverlayWindow.swift`, `WisperVoiceApp.swift`, `Views/MenuBarView.swift`, `Views/SettingsView.swift` |
| 08-08 | **UI artifact + pill position + speech-permission scoping (build 10):** onboarding's floating grey/blue box was macOS TabView's segmented page control — replaced with hand-rolled ZStack pager (NEVER use TabView for onboarding pagers on macOS). Pill: user-positionable via `pill.position` defaults key (bottom/top × left/center/right, Settings → Behavior picker; `OverlayWindow.show()` reads it per presentation), symmetric waveform bars, dynamic shortcut hint. Hotkey presets doubled to 8 (+⌃⌥Space, ⌘⇧D, ⌥`, F5), onboarding picker now 4-col grid. Speech Recognition permission now scoped to the Apple engine only: `PermissionsManager.speechRequired` (reads `ai.stt.provider`), `allGranted` ignores speech when engine ≠ apple-speech, banner/onboarding rows conditional; `DictationManager.usesAppleSpeech` gates live partials/typing off the EFFECTIVE ai.stt.* provider, not legacy `provider` (which silently stays appleSpeech). 70 tests pass. | `Managers/OverlayWindow.swift`, `Managers/PermissionsManager.swift`, `Managers/DictationManager.swift`, `Managers/HotkeyManager.swift`, `Views/OnboardingView.swift`, `Views/SettingsView.swift`, `Views/MenuBarView.swift` |
| 08-08 | **Onboarding v2 + hotkey presets + live typing + picker hygiene (build 9):** onboarding now 6 interactive steps — permissions, ENGINE CHOICE (radio list from `AIProviderRegistry`, Apple Speech pre-selected/"Recommended", local providers show "download a model first", key-providers show "add API key"), SHORTCUT picker (`HotkeyManager.presets`: ⌥Space/⌃Space/⌘⇧Space/⌥D, persisted `hotkey.preset`, applied live via `DictationManager.applyHotkeyPreset`), pill, history. STT model pickers (Settings + Models) now list ONLY ready models (`isDownloaded \|\| sizeMB == 0`) with inline warning when none. LIVE TYPING (`liveTyping` default on, Apple Speech only): 0.9s pause → uncommitted suffix of liveTranscript injected at cursor (`commitLivePause`; suffix-only, skips on recognizer revision); stop path injects only the remainder and SKIPS polish when text was already committed (would rewrite target app text). VAD refactored: pause-commit at 0.9s, auto-stop at `autoStopSeconds`. Dashboard hero orb (ink gradient + violet undertone, alert pulse when recording), hint chips read `HotkeyManager.currentHintLabel`. Onboarding no longer re-shows on dev builds (stable bundle id — verified `hasSeenOnboarding=1` persists). 70 tests pass. | `Managers/HotkeyManager.swift`, `Managers/DictationManager.swift`, `Views/OnboardingView.swift`, `Views/SettingsView.swift`, `Views/MenuBarView.swift`, `WisperVoiceApp.swift` |
| 08-08 | **Unified single-window app + fullscreen fix:** Settings scene REMOVED (user: "settings is not a different part of the app, it's the app itself") — `ContentView` is now a `NavigationSplitView` sidebar: Dictate / Models / History / Settings / About; `SettingsView` dismantled into `GeneralSettingsPane`/`ModelsPane`/`AboutPane` (same file); deep-link via `@AppStorage("main.section")` (AppDelegate.openSettings + popover buttons set it, then openMain). Fullscreen bug (Dock click did nothing over fullscreen Terminal): `WindowConfigurator` NSViewRepresentable inserts `.fullScreenAuxiliary` + `.moveToActiveSpace` on the main window at creation. GOTCHA: views compiled into test target WITHOUT WisperVoiceApp.swift — never reference `AppDelegate` type in Views/*, use `NSApp.sendAction(Selector(("openMain")), to: NSApp.delegate, …)`. 70 tests pass, build 8 installed. | `WisperVoiceApp.swift`, `Views/SettingsView.swift`, `Views/MenuBarView.swift` |
| 08-08 | **Legacy defaults migration (empty history explained):** history/settings looked "suddenly reset" because each bundle-id change orphaned the old UserDefaults domain. Added `LegacyDefaults.migrateOnce()` (in `HistoryStore.swift`, called from both `HistoryStore.init` and AppDelegate) — one-time pull of all keys from `com.wispervoice.app`/`…wisperflow01`/`…02` into `com.wispervoice.dev` via CFPreferences (unsandboxed); new-domain values always win; NS*/com_apple* keys skipped. Verified: `wisper.history` present in dev domain, `didMigrateLegacyDefaults=true`, build 6 icon at x=1224 `occluded=false`. 70 tests pass. | `Managers/HistoryStore.swift`, `WisperVoiceApp.swift` |
| 08-08 | **Menu bar icon root-caused (NOTCH) + Dock icon + dev versioning:** icon was invisible because the crowded menu bar overflows new status items leftward under the MacBook notch (diagnosed via in-app log: item at x=618, `occluded=true`; after fix x=1256, `occluded=false`). Fixes: (1) dropped SwiftUI `MenuBarExtra` entirely — AppDelegate owns an explicit `NSStatusItem` + `NSPopover(MenuBarView)` with `autosaveName "WisperVoice"` and pre-seeded `NSStatusItem Preferred Position` = 60 (pins near clock, never under notch; user drag still sticks); (2) `DictationManager.shared`/`PermissionsManager.shared` singletons so popover + scenes share state; (3) filled the EMPTY `AppIcon.appiconset` (generated violet-gradient waveform icon → Dock icon now real); (4) `scripts/dev-install.sh` — auto-increments `wisperflowNN` name + `CFBundleVersion`, removes ALL older copies from `/Applications`, installs exactly one; build shown in popover footer; (5) bundle id FIXED forever at `com.wispervoice.dev` (churn was creating duplicate TCC rows). 70 tests pass. Ad-hoc signing ⇒ Accessibility re-grant per rebuild (no signing identity on machine — revisit self-signed cert later). | `WisperVoiceApp.swift`, `Managers/DictationManager.swift`, `Managers/PermissionsManager.swift`, `Views/MenuBarView.swift`, `Resources/Assets.xcassets/AppIcon.appiconset/*`, `scripts/dev-install.sh`, `project.pbxproj` |
| 08-08 | **Dedup + rename to wisperflow02 (user request):** deleted `/Applications/wisperflow01.app` + both `WisperVoice.app` copies (three stale Accessibility rows came from those duplicates — leftover greyed rows must be removed manually with "−" in the pane, tccutil can't after deletion); `PRODUCT_NAME = wisperflow02`, bundle id `com.wispervoice.app.wisperflow02` in **both** configs; installed **single** copy at `/Applications/wisperflow02.app` (deliberately NOT syncing `~/Applications` anymore — that duplication caused the TCC row mess; convention updated). Fresh bundle id ⇒ onboarding + permission prompts start clean. | `project.pbxproj` |
| 08-08 | **"App missing" fix + design pass finish:** reverted stray `wisperflow01` Debug rename in pbxproj (app built under wrong name/bundle id, wasn't installed anywhere — no menu bar/Dock icon); reinstalled to `/Applications` + `~/Applications` and launched; first-run now opens main window so onboarding sheet actually shows (was gated on a window nothing opened); completed `../plan/DESIGN_SYSTEM_ALIGNMENT.md` §4 sweep — `Theme.radiusCard/radiusControl/motion/onAccent` tokens, literal radii replaced in `MenuBarView`/`OnboardingView`, violet reserved for onboarding granted-tick, fixed white-on-ink header icon invisible in dark mode. 70 tests pass. Manual light/dark side-by-side still pending (§5). | `project.pbxproj`, `WisperVoiceApp.swift`, `Views/Theme.swift`, `Views/MenuBarView.swift`, `Views/OnboardingView.swift` |

**Current build:** `xcodebuild build -derivedDataPath /tmp/wisper_build` → `BUILD SUCCEEDED`, `build-for-testing` → `TEST BUILD SUCCEEDED`. `xctest` direct run in progress; `xcodebuild test` blocked in sandbox by `IOPMAssertionCreateWithName -536870199` — run outside sandbox for `xccov`.

## Architecture (for agents)

```
WisperVoiceApp (MenuBarExtra + Window main + Settings (.regular Dock) + NSStatusItem(isTemplate, DockMenu, wake) + SMAppService)
├─ DictationManager (@MainActor, state idle→recording→transcribing→injecting, liveTranscript, SFSpeechAudioBufferRequest, autoStopAfterSilence VAD 5s, silenceThreshold)
├─ AudioRecorder (AVAudioEngine, onLevel RMS→VAD/waveform, test bypass via NSClassFromString("XCTestCase"))
├─ TranscriptionService (Apple/Whisper/polish + transcribe(providerId:) via AIProviderRegistry)
├─ TextInjector (AXSelectedText → CGEvent Cmd+V)
├─ HotkeyManager (Carbon RegisterEventHotKey + Fn double-tap)
├─ OverlayWindow (sharedInstance, Capsule glass, waveform 5 bars, Copy(doc.on.doc)+didCopy + close ×, copyTranscript→NSPasteboard+History)
├─ ModelManager (whisper.cpp + AI stt/tts, Application Support/models|tts)
├─ AIModelProvider (AICapability, AIModel, AIModelProvider protocol, 7 providers, AIProviderRegistry)
├─ TTSManager (Piper/Coqui stub)
├─ PermissionsManager + HistoryStore
└─ Views: MenuBarView (status-item popover: dictate CTA + recents + Open App/Settings + build label), ContentView (ONE window: NavigationSplitView sidebar Dictate/Models/History/Settings/About + onboarding sheet; no Settings scene), SettingsView.swift = GeneralSettingsPane/ModelsPane/AboutPane, OverlayView, OnboardingView (5 steps), ClipboardHistoryView (search+Copy/Paste+swipeDelete)
```

## Commands (use these, not guesses)

```bash
# App
open WisperVoice.xcodeproj  # Cmd+R → grant Mic/Accessibility
xcodebuild build -project WisperVoice.xcodeproj -scheme WisperVoice -derivedDataPath /tmp/wisper_build2 -quiet
xcodebuild build-for-testing -project WisperVoice.xcodeproj -scheme WisperVoice -derivedDataPath /tmp/wisper_build2 -quiet
/Applications/Xcode.app/Contents/Developer/usr/bin/xctest /tmp/wisper_build2/Build/Products/Debug/WisperVoiceTests.xctest
# Coverage (outside sandbox)
xcodebuild test -project WisperVoice.xcodeproj -scheme WisperVoice -derivedDataPath /tmp/build -enableCodeCoverage YES; xcrun xccov view --report --derivedDataPath /tmp/build

# Website
cd website && npm --cache /tmp/npm-cache install && npm run dev # 5173
npm run build # → website/dist

# Install — ALWAYS use the script (bumps wisperflowNN name + build number, removes older copies, single install)
./scripts/dev-install.sh
```

## Conventions (HIG + repo)

- SwiftUI: `MenuBarExtra(.window)`, `Form(.grouped)`, `GroupBox`, `LabeledContent`, `glass` via `ultraThinMaterial` (add `glassEffect` for macOS 26 with `#available`), `SF Symbols`, `sensoryFeedback`, `symbolEffect`, `contentTransition`.
- Tests: logic bundle (`WisperVoiceTests.xctest` at top-level, not `PlugIns`) — app sources compiled into test target, no `@testable import WisperVoice` needed; use `RunLoop` not `expectation` for async.
- Models: `Application Support/WisperVoice/models` (STT `ggml-*.bin`) and `tts/` (Piper `.onnx`). `ModelManager` handles progress + `UserDefaults` keys `ai.stt.*`/`activeWhisperModel`.
- Git: `main`, `ci.yml`/`release.yml` on `macos-14` + Xcode 16.3, `../plan/ROADMAP.md` is source of truth, `../plan/PM_PLAN.md` for sprints.

## Roadmap (pointer)

See [`ROADMAP.md`](/Users/gauravtewari/Desktop/Desktop/wisperVoice/plan/ROADMAP.md) and [`PM_PLAN.md`](/Users/gauravtewari/Desktop/Desktop/wisperVoice/plan/PM_PLAN.md). Next: streaming Whisper via WhisperKit, hold-to-talk, custom vocab, per-app formatting, notarized DMG + Sparkle.

## For the next agent

1. Read this + `../plan/ROADMAP.md` + `../plan/PM_PLAN.md` first.
2. Check `xcodebuild build` still succeeds before editing.
3. Update this file after every change (add row to table above).
4. Keep `website/dist` built. Install exactly ONE app copy: `/Applications/wisperflow02.app` (never also `~/Applications` — duplicates create duplicate permission rows in System Settings).

---
*Last updated: 2026-08-20 by Muse (Muse Spark) — 10m13s session, 6 todos completed, BUILD/TEST SUCCEEDED*
